{
  "name": "gem-authentication-and-ownership-tracking",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "env": "hardhat run scripts/testEnv.ts",
    "sepolia:balance": "hardhat run scripts/testSepolia.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@types/bun": "latest",
    "dotenv": "^17.2.3",
    "hardhat": "^3.1.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
